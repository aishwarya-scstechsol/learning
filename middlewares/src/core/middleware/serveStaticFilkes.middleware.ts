// import { Logger } from "@nestjs/common";
// import { Injectable, NestMiddleware } from '@nestjs/common';
// import { Request, Response, NextFunction } from 'express';
// import { join } from "path";
// import { AppConstants } from "../constants/app.constants";
// import { LoggerConstants } from "../constants/logger.constants";
// var serveStatic = require('serve-static')
// @Injectable()
// export class ServeStaticMiddleware implements NestMiddleware {
//     private logger = new Logger (ServeStaticMiddleware.name)
//     use(req: any, res: any, next: (error?: any) => void) {
//         this.logger.log(LoggerConstants.SERVE_STATIC_FILES_MIDDLEWARE)
//         var serve = serveStatic(join(__dirname, '..', 'files'))
//         return serve
         
          
//         next()
//     }

// }





